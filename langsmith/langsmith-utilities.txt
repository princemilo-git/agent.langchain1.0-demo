langsmith: 日志工具
    1）smith.langchain.com 注册账户，申请api key
    2）程序里边使用.env，定义环境变量
studio：部署agent
    1）前提：langsmith已经安装
    2）安装：输入指令：pip install langgraph-cli
    3）编辑：根目录下的langgraph.json，指定agent、env环境配置
    4）启动：工作目录下，输入指令：langgraph dev # D:\Work\Workspace\AIProjects\Agent\langchain1.0-demo\
    5）测试：访问：https://smith.langchain.com/studio/，切换到chat模式，进行提问交互
agent chat-ui：开发前端ui工程 和部署agent交互
    0）前提：部署agent http://localhost:2024
    1）启动：node.js command prompt
    2）创建前端ui工程：
       # For customization or local development
       # Create a new Agent Chat UI project
       npx create-agent-chat-app --project-name my-chat-ui
    3）cd my-chat-ui
       # D:\Work\Workspace\AIProjects\Agent\langchain1.0-demo\my-chat-ui
    4）npm install        # Install dependencies and start
    5）npm run dev
    6）测试：访问：http://localhost:5173